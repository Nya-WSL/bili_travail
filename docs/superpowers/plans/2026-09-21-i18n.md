# 多语言（i18n）实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 让加班姬界面支持简体中文与 English，默认跟随系统语言，设置页可手动切换（重启生效），日志保持中文不变。

**架构：** 新增 `libs/i18n.py` 作为唯一翻译入口，读取 `locales/<语言代码>.json` 扁平 key 资源；语言列表由扫描 `locales/` 目录自动产生，每个语言文件自带母语名；界面文案全部改为 `t(key)` 调用，缺失时回退 `zh-CN`。

**技术栈：** Python 3.13、NiceGUI 3.15（其 `ui.run(language=...)` 同步 Quasar 语言）、orjson（项目已有）、tomlkit（配置）

**测试策略说明：** 项目当前无 pytest 依赖与 `tests/` 目录，本计划不引入测试基建，改用可执行的 `python -c` 内联断言作为每步验证手段。

---

## 文件结构

| 文件 | 职责 |
|---|---|
| `libs/i18n.py` | **新建**。加载语言文件、`t()` 翻译、系统语言检测、`available_languages()` 扫描目录 |
| `locales/zh-CN.json` | **新建**。简体中文资源（默认语言与回退语言） |
| `locales/en-US.json` | **新建**。English 资源 |
| `libs/config.py` | 修改：`_build_default_config()` 增加 `[general] language = "auto"` |
| `main.py` | 修改：启动集成、设置页语言下拉、约 100 处文案改为 `t()` |
| `pages/about.py` | 修改：5 处静态文案 |
| `pages/count.py` | 修改：4 处文案 |
| `libs/update.py` | 修改：13 处下载/校验提示 |
| `libs/countdown_timer.py` | 修改：1 处提示 |
| `libs/format.py` | 修改：`format_seconds` 时间单位 |
| `build.py` | 修改：打包时拷贝 `locales/` |

经核查**无需改造**：`libs/check_runtime.py`（仅日志）、`libs/changelog.py`（仅 "GitHub" 按钮，条目正文不翻译）、`pages/capture_cd.py`（无静态中文，时间文案来自 `libs/format.py`）。

---

## 任务 1：i18n 核心模块

**文件：**
- 创建：`libs/i18n.py`

- [ ] **步骤 1：运行验证命令确认模块不存在**

```bash
python -c "from libs import i18n; print(i18n.t('main.btn.start'))"
```

预期：FAIL，`ModuleNotFoundError: No module named 'libs.i18n'`

- [ ] **步骤 2：创建 `libs/i18n.py`**

```python
import os
import locale as _locale
from pathlib import Path

import orjson

from .log import logger

DEFAULT_LANG = "zh-CN"
LOCALES_DIR = Path("locales")
LANG_NAME_KEY = "__language_name__"

# 下拉展示顺序：内置语言在前，其余按语言代码升序
_ORDER = ["zh-CN", "en-US"]
# NiceGUI(Quasar) 支持的语言代码，未收录的语言回退英文
_UI_LANG_MAP = {"zh-CN": "zh-CN", "en-US": "en-US"}

_translations: dict[str, dict[str, str]] = {}
_current_lang = DEFAULT_LANG


def _load(lang: str) -> dict[str, str]:
    """加载语言文件并缓存，失败时缓存空字典"""
    if lang in _translations:
        return _translations[lang]

    path = LOCALES_DIR / f"{lang}.json"

    try:
        with open(path, "rb") as f:
            data = orjson.loads(f.read())
        _translations[lang] = data
    except Exception:
        logger.warning("加载语言文件失败，将回退默认语言: {}", path)
        _translations[lang] = {}

    return _translations[lang]


def available_languages() -> dict[str, str]:
    """扫描 locales/*.json，返回 {语言代码: 母语名}，每次调用重新扫描"""
    langs: dict[str, str] = {}

    if not LOCALES_DIR.exists():
        return {DEFAULT_LANG: DEFAULT_LANG}

    for path in sorted(LOCALES_DIR.glob("*.json")):
        code = path.stem
        langs[code] = _load(code).get(LANG_NAME_KEY, code)

    if not langs:
        return {DEFAULT_LANG: DEFAULT_LANG}

    ordered = {code: langs[code] for code in _ORDER if code in langs}
    ordered.update({code: name for code, name in sorted(langs.items()) if code not in _ORDER})
    return ordered


def detect_system_language() -> str:
    """检测系统语言并映射为受支持的语言代码"""
    try:
        if os.name == "nt":
            import ctypes
            # LANGID 主语言位：0x04=zh，0x09=en
            primary = ctypes.windll.kernel32.GetUserDefaultUILanguage() & 0x3FF
            if primary == 0x04:
                return "zh-CN"
            if primary == 0x09:
                return "en-US"
            return DEFAULT_LANG

        lang = (_locale.getdefaultlocale()[0] or os.environ.get("LANG", "")).lower()
        if lang.startswith("zh"):
            return "zh-CN"
        if lang.startswith("en"):
            return "en-US"
    except Exception:
        logger.warning("检测系统语言失败，使用默认语言")

    return DEFAULT_LANG


def resolve_language(value: str | None) -> str:
    """配置值解析：auto/空 走系统检测"""
    if not value or value == "auto":
        return detect_system_language()
    return value


def set_language(lang: str) -> None:
    """设置当前语言，不受支持时回退默认语言"""
    global _current_lang

    if lang not in available_languages():
        logger.warning("语言 {} 不存在，回退为 {}", lang, DEFAULT_LANG)
        lang = DEFAULT_LANG

    _current_lang = lang
    _load(lang)


def get_language() -> str:
    return _current_lang


def ui_language() -> str:
    """供 ui.run(language=...) 使用的 Quasar 语言代码"""
    return _UI_LANG_MAP.get(_current_lang, "en-US")


def t(key: str, **kwargs) -> str:
    """翻译：当前语言 -> 默认语言 -> key 原文"""
    text = _load(_current_lang).get(key)

    if text is None:
        text = _load(DEFAULT_LANG).get(key)

    if text is None:
        logger.debug("缺少翻译: {}", key)
        return key

    if kwargs:
        try:
            return text.format(**kwargs)
        except Exception:
            logger.warning("翻译占位符填充失败: {}", key)

    return text
```

- [ ] **步骤 3：运行验证命令确认模块可导入且回退 key**

```bash
python -c "from libs import i18n; i18n.set_language('en-US'); print(i18n.t('main.btn.start'), '|', i18n.available_languages(), '|', i18n.detect_system_language())"
```

预期：`main.btn.start | {'zh-CN': 'zh-CN'} | zh-CN`（locales 目录尚未创建，扫描结果回退默认语言；`t` 返回 key 原文）

- [ ] **步骤 4：Commit**

```bash
git add libs/i18n.py
git commit -m "feat(i18n): 新增翻译模块" -m "- 支持 locales/*.json 扁平 key 资源加载与缓存" -m "- 扫描目录生成语言列表，缺失回退简体中文" -m "- 提供系统语言检测与 ui.run 语言代码映射"
```

---

## 任务 2：语言资源文件骨架

**文件：**
- 创建：`locales/zh-CN.json`
- 创建：`locales/en-US.json`

本任务先建立 `common.*`、`settings.*`、`format.*` 三组的 key，后续任务按组追加。

- [ ] **步骤 1：创建 `locales/zh-CN.json`**

```json
{
  "__language_name__": "简体中文",
  "common.confirm": "确定",
  "common.cancel": "取消",
  "common.close": "关闭",
  "common.delete": "删除",
  "common.submit": "提交",
  "common.load": "加载",
  "common.save": "保存",
  "common.clear": "清空",
  "common.yes": "是",
  "common.no": "否",
  "settings.language": "语言",
  "settings.language_auto": "跟随系统",
  "settings.language_restart": "语言已保存，重启程序后生效",
  "format.hours": "{hours}小时",
  "format.minutes": "{minutes}分",
  "format.seconds": "{seconds}秒"
}
```

- [ ] **步骤 2：创建 `locales/en-US.json`**

```json
{
  "__language_name__": "English",
  "common.confirm": "OK",
  "common.cancel": "Cancel",
  "common.close": "Close",
  "common.delete": "Delete",
  "common.submit": "Submit",
  "common.load": "Load",
  "common.save": "Save",
  "common.clear": "Clear",
  "common.yes": "Yes",
  "common.no": "No",
  "settings.language": "Language",
  "settings.language_auto": "Follow system",
  "settings.language_restart": "Language saved. Restart the app to apply.",
  "format.hours": "{hours}h",
  "format.minutes": "{minutes}m",
  "format.seconds": "{seconds}s"
}
```

- [ ] **步骤 3：验证两个文件可解析且 key 集合一致**

```bash
python -c "import orjson;a=orjson.loads(open('locales/zh-CN.json','rb').read());b=orjson.loads(open('locales/en-US.json','rb').read());print(sorted(set(a)-set(b)) or 'keys match');print(len(a))"
```

预期：`keys match`，然后输出 key 数量 `17`（1 个 `__language_name__` + 10 个 common + 3 个 settings + 3 个 format）

- [ ] **步骤 4：Commit**

```bash
git add locales
git commit -m "feat(i18n): 新增简体中文与英文语言资源" -m "- 扁平 key，含 __language_name__ 母语名" -m "- 首批覆盖通用按钮、语言设置与时间单位"
```

---

## 任务 3：配置项 language

**文件：**
- 修改：`libs/config.py:10-23`（`_build_default_config` 的 general 表）

- [ ] **步骤 1：在 general 表增加 language 键**

在 `general.add("auth_code", "")` 与 `general["auth_code"].comment("主播身份码")` 之后追加：

```python
    general.add("language", "auto")
    general["language"].comment("界面语言，auto 为跟随系统")
```

- [ ] **步骤 2：验证已有配置自动补齐该键**

```bash
python -c "from libs.config import Config; c=Config(); c.sync_config(); print(c.get('general','language'))"
```

预期：`auto`（若 config.toml 原本没有该键，sync_config 会写入默认值）

- [ ] **步骤 3：Commit**

```bash
git add libs/config.py
git commit -m "feat(i18n): 配置新增界面语言项" -m "- [general] language，默认 auto 跟随系统"
```

---

## 任务 4：启动集成

**文件：**
- 修改：`main.py:41-51`（import 区）、`main.py:181-183`（配置加载后）、`main.py:2259-2271`（`ui.run` 调用）

- [ ] **步骤 1：在 import 区引入 i18n**

在 `from libs import gift as get_gift` 之前追加：

```python
from libs import i18n
from libs.i18n import t
```

- [ ] **步骤 2：配置加载后解析并设置语言**

在 `config = base_config.load()` 之后追加：

```python
i18n.set_language(i18n.resolve_language(config["general"].get("language", "auto")))
logger.debug("界面语言: {}", i18n.get_language())
```

- [ ] **步骤 3：`ui.run` 同步 Quasar 语言**

将 `ui.run(` 参数中的 `language="zh-CN"` 改为 `language=i18n.ui_language()`。

- [ ] **步骤 4：验证语言解析生效**

```bash
python -c "from libs import i18n; print(i18n.resolve_language('auto'), i18n.resolve_language('en-US'), i18n.ui_language())"
```

预期：输出系统检测结果（如 `zh-CN`）、`en-US`、`zh-CN`

- [ ] **步骤 5：Commit**

```bash
git add main.py
git commit -m "feat(i18n): 启动时按配置解析界面语言" -m "- 支持 auto 跟随系统，并同步 Quasar 组件语言"
```

---

## 任务 5：设置页语言下拉

**文件：**
- 修改：`main.py` 的「程序设置」tab（`ui.tab_panel("6")` 区域，约 2140 行）

- [ ] **步骤 1：在「程序设置」tab 内增加语言选择行**

在「自动检查更新」开关所在行之后追加：

```python
                with ui.row(align_items="center"):
                    def language_options() -> dict:
                        return {"auto": t("settings.language_auto"), **i18n.available_languages()}

                    lang_select = ui.select(
                        label=t("settings.language"),
                        options=language_options(),
                        value=config["general"].get("language", "auto"),
                    ).style("width: 160px")

                    def change_language(e):
                        config["general"]["language"] = e.value
                        base_config.save(config)
                        ui.notify(t("settings.language_restart"), type="info")

                    lang_select.on_value_change(change_language)

                    def refresh_language_options():
                        # 重新扫描 locales 目录，运行中新增的语言文件也能出现
                        lang_select.set_options(language_options())

                    refresh_language_options()
```

- [ ] **步骤 2：切到「程序设置」tab 时刷新下拉**

在创建 tabs 后（`with ui.tabs() as tabs:` 块之后）追加：

```python
        tabs.on_value_change(lambda e: refresh_language_options() if e.value == "6" else None)
```

（`refresh_language_options` 需定义在 tab_panel("6") 内，若作用域不可达，改为把该函数提升到 `index()` 内 tabs 定义之前，并用 `global` 或在 `index()` 顶层定义 `lang_select` 后绑定；实现时以可运行为准。）

- [ ] **步骤 3：验证下拉选项由目录产生**

```bash
python -c "from libs import i18n; print(i18n.available_languages())"
```

预期：`{'zh-CN': '简体中文', 'en-US': 'English'}`

- [ ] **步骤 4：Commit**

```bash
git add main.py
git commit -m "feat(i18n): 设置页新增语言选择" -m "- 选项由 locales 目录扫描产生，含跟随系统" -m "- 切换后写回配置并提示重启生效"
```

---

## 任务 6：主界面静态文案

**文件：**
- 修改：`main.py`（tab 标题字典、按钮、输入标签、开关、tooltip、颜色标签）

- [ ] **步骤 1：向两个语言文件追加 `main.*` key**

| key | zh-CN | en-US |
|---|---|---|
| `main.tab.account` | 账号设置 | Account |
| `main.tab.gift` | 礼物设置 | Gifts |
| `main.tab.display` | 显示设置 | Display |
| `main.tab.appearance` | 外观设置 | Appearance |
| `main.tab.stats` | 统计相关 | Statistics |
| `main.tab.program` | 程序设置 | Settings |
| `main.tab.simulator` | 模拟测试 | Simulator |
| `main.btn.start` | 开始 | Start |
| `main.btn.pause` | 暂停 | Pause |
| `main.btn.resume` | 继续 | Resume |
| `main.btn.stop` | 停止 | Stop |
| `main.btn.add` | 增加 | Add |
| `main.btn.sub` | 减少 | Reduce |
| `main.btn.save` | 保存 | Save |
| `main.btn.load` | 读取 | Load |
| `main.btn.gift_setting` | 设置礼物 | Gift rules |
| `main.btn.gift_refresh` | 更新礼物 | Refresh gifts |
| `main.btn.blind_box` | 盲盒盈亏 | Blind box P&L |
| `main.btn.gift_stats` | 礼物统计 | Gift statistics |
| `main.btn.login` | 登录账号 | Login |
| `main.btn.check_update` | 检查更新 | Check update |
| `main.btn.changelog` | 更新日志 | Changelog |
| `main.btn.upload_log` | 上传日志 | Upload log |
| `main.input.hour` | 时 | h |
| `main.input.minute` | 分 | m |
| `main.input.second` | 秒 | s |
| `main.input.auth_code` | 身份码 | Auth code |
| `main.label.room_id` | 房间号： | Room: |
| `main.status.disconnected` | 未连接 | Not connected |
| `main.switch.connect` | 连接至弹幕服务器 | Connect to danmaku server |
| `main.switch.ignore_cd` | 忽略倒计时 | Ignore countdown |
| `main.switch.exit_timer` | 倒计时结束后断开连接 | Disconnect when countdown ends |
| `main.switch.capture_rank` | OBS显示排行榜 | OBS: show ranking |
| `main.switch.capture_gift` | OBS显示投喂记录 | OBS: show gift feed |
| `main.switch.borderless_cd` | 无边框倒计时 | Borderless countdown |
| `main.switch.auto_update` | 自动检查更新 | Auto check update |
| `main.color.time` | 计时颜色 | Countdown color |
| `main.color.btn` | 按钮颜色 | Button color |
| `main.color.bg` | 背景颜色 | Background color |
| `main.color.main_text` | 主界面字体颜色 | Main text color |
| `main.color.sub_text` | 子页面字体颜色 | Sub page text color |
| `main.tooltip.ignore_cd` | 启用时在倒计时结束后（包括暂停时）仍然会触发加减时，与倒计时结束后断开连接互斥 | When enabled, gifts still change the countdown after it ends (paused included). Mutually exclusive with "Disconnect when countdown ends". |
| `main.tooltip.exit_timer` | 倒计时结束后是否断开弹幕服务器连接，与忽略倒计时互斥 | Whether to disconnect from the danmaku server when the countdown ends. Mutually exclusive with "Ignore countdown". |
| `main.tooltip.exit_delay` | 倒计时结束后断开连接的延迟时间，单位为秒 | Delay in seconds before disconnecting after the countdown ends |
| `main.tooltip.borderless` | 启用时OBS页面倒计时将不显示边框，仅显示数字 | When enabled, the OBS countdown shows numbers only, without border |

- [ ] **步骤 2：替换 `main.py` 中对应硬编码文案**

示例（其余按同表逐项替换）：

```python
        content = {
            "1": t("main.tab.account"), "2": t("main.tab.gift"), "3": t("main.tab.display"),
            "4": t("main.tab.appearance"), "5": t("main.tab.stats"), "6": t("main.tab.program"),
            "7": t("main.tab.simulator"),
        }
```

```python
            start_button = ui.button(t('main.btn.start'), on_click=lambda: start_task())
            input_hour = ui.number(t("main.input.hour"), value=0, min=0).style("width: 100px")
            auth_code = ui.input(t("main.input.auth_code"), on_change=lambda: base_config.save(config), password=True, password_toggle_button=True).style("width: 120px")
            ui.label(t("main.label.room_id"))
            login_status = ui.label(t("main.status.disconnected")).classes("text-red")
            b_connect_switch = ui.switch(t("main.switch.connect"), on_change=lambda: check_b_connect_status())
            ui.tooltip(t("main.tooltip.ignore_cd"))
```

注意：`ui.button('停止', ...)` 等既有单引号写法一并替换；`login_status` 后续 `set_text("未连接")` 的两处（`check_b_connect_status`、`_on_heartbeat`）同样改为 `t("main.status.disconnected")`。

- [ ] **步骤 3：验证无遗漏**

```bash
python -c "
import re
s=open('main.py',encoding='utf-8').read()
pat=re.compile(r'ui\.(label|button|switch|number|input|tooltip)\(\s*[\'\"]')
hits=[l.strip()[:90] for l in s.splitlines() if pat.search(l) and re.search(r'[\u4e00-\u9fff]',l)]
print(len(hits)); [print(h) for h in hits]"
```

预期：`0`（主界面静态文案已全部替换；弹窗与通知在后续任务处理，此处可能仍有输出，需逐条确认属于后续任务范围）

- [ ] **步骤 4：Commit**

```bash
git add main.py locales
git commit -m "feat(i18n): 主界面文案接入翻译" -m "- tab 标题、按钮、输入标签、开关与提示均已接入"
```

---

## 任务 7：通知与弹窗文案

**文件：**
- 修改：`main.py`（47 处 `ui.notify` 与各弹窗文案）、`locales/*.json`

- [ ] **步骤 1：向两个语言文件追加 `notify.*` / `dialog.*` key**

| key | zh-CN | en-US |
|---|---|---|
| `notify.waiting_gift_data` | 正在等待B站下发自定义礼物数据，请稍候... | Waiting for custom gift data from Bilibili... |
| `notify.gift_data_failed` | 获取礼物数据失败，可能导致部分功能异常 | Failed to fetch gift data, some features may not work |
| `notify.select_gift` | 请选择礼物 | Please select a gift |
| `notify.time_not_negative` | 时间不能是负数 | Time cannot be negative |
| `notify.random_range_invalid` | 随机的值必须最小数<=最大数 | Min must be <= Max |
| `notify.random_empty` | 随机的值为空 | Random range is empty |
| `notify.start_timer_first` | 请先开始计时 | Please start the countdown first |
| `notify.disconnected` | 已断开连接 | Disconnected |
| `notify.connect_timeout` | 连接超时，请检查日志 | Connection timed out, check the log |
| `notify.no_auth_code_connect` | 未填入身份码，无法连接弹幕服务器 | No auth code, cannot connect to the danmaku server |
| `notify.enter_auth_code` | 请输入身份码 | Please enter the auth code |
| `notify.updating_gift` | 正在更新礼物数据，请稍后... | Updating gift data... |
| `notify.gift_updated` | 礼物数据更新完成 | Gift data updated |
| `notify.gift_init_local` | 未检测到本地礼物数据，将初始化礼物数据... | No local gift data, initializing... |
| `notify.gift_initialized` | 礼物数据初始化完成 | Gift data initialized |
| `notify.gift_update_failed` | 礼物数据更新失败，请检查日志或稍后重试，或者使用本地数据重置 | Failed to update gift data. Check the log, retry later, or reset from local data |
| `notify.reset_success` | 重置成功 | Reset successfully |
| `notify.reset_failed` | 重置失败 | Reset failed |
| `notify.update_source_failed` | 获取更新源失败，尝试自动检测可用更新源 | Failed to get update sources, detecting automatically |
| `notify.speed_testing` | 测速中，请稍候... | Testing speed... |
| `notify.update_server_unreachable` | 无法连接更新服务器 | Cannot reach the update server |
| `notify.update_source_empty` | 更新源为空，将尝试从Github获取更新 | No update source, falling back to GitHub |
| `notify.check_update_failed` | 检查更新失败 | Failed to check for updates |
| `notify.already_latest` | 已是最新版本 | Already up to date |
| `notify.auto_update_off` | 已关闭自动检查更新 | Auto update check is disabled |
| `notify.saved` | 保存成功 | Saved |
| `notify.name_required` | 名称不能为空 | Name cannot be empty |
| `notify.loaded` | 加载成功 | Loaded |
| `notify.deleted` | 删除成功 | Deleted |
| `notify.blind_box_cleared` | 盲盒统计已清空 | Blind box statistics cleared |
| `dialog.reset_all_gifts` | 是否确认重置所有礼物？ | Reset all gift rules? |
| `dialog.confirm_reset` | 确认重置 | Confirm reset |
| `dialog.cancel_reset` | 取消重置 | Cancel |
| `dialog.confirm_clear_blind_box` | 是否确认清空所有盲盒统计？ | Clear all blind box statistics? |
| `dialog.confirm_clear` | 确认清空 | Confirm |
| `dialog.overwrite_name` | 该名称已存在，是否覆盖？ | This name already exists. Overwrite? |
| `dialog.save_name` | 保存名称 | Name |
| `dialog.select_name` | 选择名称 | Select name |
| `dialog.update_gift_confirm` | 是否进行更新？ | Update now? |
| `dialog.reset_local_gift` | 使用本地数据重置 | Reset from local data |
| `dialog.gift_preview` | 设置预览 | Preview |
| `dialog.blind_box_price` | {box} \| 价格：{price}电池 | {box} \| Price: {price} batteries |
| `dialog.blind_box_gift` | 礼物：{name} \| 数量：{num} \| 总价格：{total}电池 | Gift: {name} \| Count: {num} \| Total: {total} batteries |
| `dialog.blind_box_profit` | 盈亏：{profit}电池 | Profit: {profit} batteries |
| `dialog.blind_box_total_profit` | 总盈亏：{profit}电池 | Total profit: {profit} batteries |

- [ ] **步骤 2：替换 `main.py` 中的 notify 与弹窗文案**

```python
            ui.notify(t("notify.gift_updated"), type="positive")
            ui.label(t("dialog.reset_all_gifts"))
            ui.button(t("dialog.confirm_reset"), on_click=lambda: double_check())
            ui.label(t("dialog.blind_box_price", box=box_name, price=gift_price_list[box_name]))
            ui.label(t("dialog.blind_box_profit", profit=price_list[k]))
            ui.label(t("dialog.blind_box_total_profit", profit=all_price))
```

`upload_log` 中剩余的上传结果提示（`日志上传成功，状态码：{status}`、`日志上传失败，状态码: {status}`、`未配置服务器地址，上传日志失败`、`文件不存在: {path}`）同样按此模式新增 `notify.log_uploaded` / `notify.log_upload_failed` / `notify.no_server` / `notify.file_missing` 四个 key。

- [ ] **步骤 3：验证 notify 无中文残留**

```bash
python -c "
import re
s=open('main.py',encoding='utf-8').read()
hits=[l.strip()[:90] for l in s.splitlines() if 'ui.notify(' in l and re.search(r'[\u4e00-\u9fff]',l)]
print(len(hits)); [print(h) for h in hits]"
```

预期：`0`

- [ ] **步骤 4：Commit**

```bash
git add main.py locales
git commit -m "feat(i18n): 通知与弹窗文案接入翻译" -m "- 47 处通知与盲盒/礼物/更新等弹窗文案接入" -m "- 含带占位符的盈亏文案"
```

---

## 任务 8：工单、模拟测试器与礼物设置弹窗

**文件：**
- 修改：`main.py`（工单 dialog、模拟测试 tab、礼物设置 dialog）、`locales/*.json`

- [ ] **步骤 1：追加 key**

| key | zh-CN | en-US |
|---|---|---|
| `ticket.submit_title` | 提交工单 | Submit ticket |
| `ticket.type` | 工单类型 | Type |
| `ticket.type.bug` | Bug反馈 | Bug report |
| `ticket.type.feature` | 功能请求 | Feature request |
| `ticket.title` | 标题 | Title |
| `ticket.title_placeholder` | 请输入工单标题 | Enter ticket title |
| `ticket.description` | 详细描述 | Description |
| `ticket.description_placeholder` | 请详细描述您遇到的问题或期望的功能... | Describe the issue or the feature you want... |
| `ticket.my_tickets` | 我的工单 | My tickets |
| `ticket.empty` | 暂无工单记录 | No tickets yet |
| `sim.gift` | 礼物选择 | Gift |
| `sim.num` | 数量 | Count |
| `sim.price` | 单价(电池) | Unit price (batteries) |
| `sim.uname` | 用户名 | Username |
| `sim.blind_box_switch` | 盲盒礼物 | Blind box gift |
| `sim.box` | 盲盒选择 | Blind box |
| `sim.send` | 发送模拟礼物 | Send simulated gift |
| `sim.ready` | 就绪 - 请先开始倒计时，再发送模拟礼物 | Ready - start the countdown first, then send |
| `sim.select_gift` | 请选择礼物 | Please select a gift |
| `sim.select_box` | 请选择盲盒 | Please select a blind box |
| `sim.tooltip_box` | 模拟从盲盒中开出的礼物，「礼物选择」为开出的礼物，「盲盒选择」为其所属的盲盒 | Simulate a gift from a blind box: "Gift" is the revealed gift, "Blind box" is its source |
| `gift.select` | 礼物选择 | Gift |
| `gift.random_min` | 随机最小数(秒) | Min (s) |
| `gift.random_max` | 随机最大数(秒) | Max (s) |
| `gift.duration` | 时长(秒) | Duration (s) |
| `gift.random_weight` | 随机玩法权重设置(设置会自动保存) - BETA | Random weights (auto-saved) - BETA |
| `gift.usage_doc` | 使用说明 | Usage |
| `gift.rate_nega` | 减时概率 | Reduce chance |
| `gift.rate_zero` | 零的概率 | No-change chance |
| `gift.rate_posi` | 加时概率 | Add chance |
| `gift.play.add` | 加时 | Add |
| `gift.play.sub` | 减时 | Reduce |
| `gift.play.double` | 加倍 | Double |
| `gift.play.half` | 减半 | Halve |
| `gift.play.clear` | 清空 | Clear |
| `gift.play.random` | 随机 | Random |
| `gift.reset_all` | 重置全部 | Reset all |

- [ ] **步骤 2：替换对应文案**

```python
        status = ui.toggle(options={"add": t("gift.play.add"), "sub": t("gift.play.sub"), "double": t("gift.play.double"), "half": t("gift.play.half"), "clear": t("gift.play.clear"), "random": t("gift.play.random")}, on_change=lambda: show())
        sim_gift_select = ui.select(label=t("sim.gift"), options=[], with_input=True, clearable=True).style("width: 160px")
        ui.button(t("sim.send"), on_click=lambda: asyncio.ensure_future(do_simulate_gift()))
```

- [ ] **步骤 3：验证主文件已无硬编码界面文案**

```bash
python -c "
import re
s=open('main.py',encoding='utf-8').read()
hits=[l.strip()[:90] for l in s.splitlines() if re.search(r'ui\.\w+\(',l) and re.search(r'[\u4e00-\u9fff]',l) and 'logger' not in l]
print(len(hits)); [print(h) for h in hits]"
```

预期：`0`

- [ ] **步骤 4：Commit**

```bash
git add main.py locales
git commit -m "feat(i18n): 工单、模拟测试器与礼物设置文案接入翻译"
```

---

## 任务 9：子页面与库内提示

**文件：**
- 修改：`pages/about.py`、`pages/count.py`、`libs/update.py`、`libs/countdown_timer.py`、`libs/format.py`、`locales/*.json`

- [ ] **步骤 1：追加 key**

| key | zh-CN | en-US |
|---|---|---|
| `about.development` | 程序开发 | Development |
| `about.thanks` | 特别鸣谢 | Special thanks |
| `about.contact` | 联系我们 | Contact |
| `about.group` | Nya-WSL服务与反馈群 | Nya-WSL feedback group |
| `about.docs` | 使用文档 | Documentation |
| `about.chat_load_failed` | 加载聊天消息失败，请稍后再试 | Failed to load messages, please try again later |
| `count.gift_unit` | {num}个 / {price}电池 | {num} / {price} batteries |
| `count.senders` | 送礼用户 | Senders |
| `count.total` | 总计：{num}个礼物 / {price}电池 | Total: {num} gifts / {price} batteries |
| `update.downloading` | 正在下载更新包 | Downloading update... |
| `update.failed` | 更新失败 | Update failed |
| `update.failed_reason` | 更新失败：{status} {reason} | Update failed: {status} {reason} |
| `update.progress` | 下载进度：{percent} | Downloading: {percent} |
| `update.downloading_bytes` | 下载中... 已下载 {bytes} 字节 | Downloading... {bytes} bytes |
| `update.download_done` | 下载完成！ | Download complete! |
| `update.checking_sha` | 正在校验SHA256... | Verifying SHA256... |
| `update.sha_failed` | 更新失败：SHA256校验未通过，请检查日志 | Update failed: SHA256 mismatch, check the log |
| `update.extracting` | 正在解压更新包... | Extracting... |
| `update.updating` | 正在更新... | Updating... |
| `cd.enter_time` | 请输入时间 | Please enter a time |

- [ ] **步骤 2：`libs/format.py` 的时间单位改为翻译**

```python
from libs.i18n import t
...
    if hours > 0:
        parts.append(t("format.hours", hours=hours))
    if minutes > 0:
        parts.append(t("format.minutes", minutes=minutes))
    if seconds > 0 or (hours == 0 and minutes == 0):
        parts.append(t("format.seconds", seconds=seconds))
```

- [ ] **步骤 3：替换 `pages/about.py`、`pages/count.py`、`libs/update.py`、`libs/countdown_timer.py` 文案**

```python
ui.label(t("about.development")).classes("text-blue")
ui.label(t("count.senders"))
ui.label(t("count.total", num=total_num, price=total_price))
ui.notify(t("update.failed"), type="negative")
percent_dialog.set_text(t("update.downloading"))
ui.notify(t("cd.enter_time"), type="negative")
```

- [ ] **步骤 4：验证时间格式化**

```bash
python -c "from libs import i18n; from libs.format import format_seconds; i18n.set_language('zh-CN'); print(format_seconds(3725)); i18n.set_language('en-US'); print(format_seconds(3725))"
```

预期：`+1小时2分5秒` 与 `+1h2m5s`

- [ ] **步骤 5：Commit**

```bash
git add pages libs locales
git commit -m "feat(i18n): 子页面与库内提示接入翻译" -m "- about/count 页面、更新流程提示、时间单位" -m "- 时间单位随语言切换（1小时2分5秒 / 1h2m5s）"
```

---

## 任务 10：打包携带语言资源

**文件：**
- 修改：`build.py:176-178`（资源拷贝区）

- [ ] **步骤 1：在拷贝 static 之后追加 locales 拷贝**

```python
    shutil.copytree("locales", Path("dist", "start", "locales"), dirs_exist_ok=True)
```

- [ ] **步骤 2：验证拷贝逻辑**

```bash
python -c "
from pathlib import Path
import shutil, tempfile, os
tmp=Path(tempfile.mkdtemp())
shutil.copytree('locales', tmp/'locales', dirs_exist_ok=True)
print(sorted(p.name for p in (tmp/'locales').glob('*.json')))"
```

预期：`['en-US.json', 'zh-CN.json']`

- [ ] **步骤 3：Commit**

```bash
git add build.py
git commit -m "build: 打包时携带 locales 语言资源" -m "- 与 static 同样拷入 dist/start，保证升级包可用"
```

---

## 任务 11：端到端验证

**文件：** 无新增文件（必要时调整上一步遗漏的文案）

- [ ] **步骤 1：英文模式启动走查**

临时将 `config.toml` 的 `[general] language` 改为 `"en-US"`，启动程序，逐项确认：主界面 tab/按钮/开关/tooltip、礼物设置弹窗、盲盒盈亏弹窗、模拟测试器、about/count 页面、OBS 页面时间单位均为英文，无中文残留。

- [ ] **步骤 2：系统语言自动切换**

将 `language` 改回 `"auto"`，在中文系统与英文系统下分别启动，确认自动切换正确。

- [ ] **步骤 3：缺文件回退**

临时把 `locales/en-US.json` 改名，确认程序仍以中文启动不报错，下拉只剩「跟随系统 / 简体中文」。验证后改回。

- [ ] **步骤 4：运行中新增语言**

复制 `locales/en-US.json` 为 `locales/ja-JP.json`（内容保持英文亦可），切到「程序设置」tab，确认下拉出现 `ja-JP`（名称取自文件内 `__language_name__`）。验证后删除该文件。

- [ ] **步骤 5：日志仍为中文**

检查 `logs/bili_travail_*.log`，确认日志文案未被翻译。

- [ ] **步骤 6：Commit（如有修正）**

```bash
git add main.py pages libs locales
git commit -m "fix(i18n): 修正走查发现的漏翻文案"
```

---

## 自检记录

- 规格覆盖度：架构、目录扫描、系统检测、配置、设置页、文案替换、打包、验证均已对应任务；规格第 9 节中的 `check_runtime.py` 与 `changelog.py` 经核查无用户可见文案，已在文件结构中标注不改造
- 占位符：全部任务均给出实际代码、key 表与验证命令，无「待定 / 类似任务 N」
- 类型一致性：`t()`、`set_language()`、`resolve_language()`、`available_languages()`、`ui_language()` 的签名贯穿各任务，密钥名统一为 `language`、`__language_name__`
